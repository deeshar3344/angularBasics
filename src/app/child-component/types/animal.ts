export class Animal {
  color: string;
  height: number;
  name: string;

  constructor(color: string, height: number, name: string) {
    this.color = color;
    this.height = height;
    this.name = name;
  }
}
